---

dependencies:

  - role: requirements
    pip2_requirements:
      - docker-py
    when: docker_has_run is undefined

#   - role: ufw
#     ufw_rules: "{{ docker_ufw_rules }}"
#     when: docker_has_run is undefined and docker_ufw_rules
