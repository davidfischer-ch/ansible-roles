---

dependencies:
  - role: requirements
    key_requirements:
      - http://download.opensuse.org/repositories/isv:ownCloud:desktop/xUbuntu_{{ ansible_distribution_version }}/Release.key
    when: owncloud_client_has_run is undefined and ansible_distribution == 'Ubuntu' and ansible_distribution_major_version|int < 15
