mesos_packages:
  - autoconf
  - libapr1-dev
  - libc6-dev
  - libcurl4-nss-dev
  - libsasl2-dev
  - libsasl2-modules
  - libsvn-dev
  - libtool
  - maven
  - zlib1g-dev
mesos_python_packages:
  - six
mesos_package_url: https://www.apache.org/dist/mesos/{{ mesos_version }}/mesos-{{ mesos_version }}.tar.gz
