---

dependencies:
  - role: requirements
    apt_requirements:
      - nfs-kernel-server
    when: nfs_has_run is undefined

  - role: ufw
    ufw_rules: '{{ nfs_ufw_rules }}'
    when: nfs_has_run is undefined and nfs_ufw_rules
