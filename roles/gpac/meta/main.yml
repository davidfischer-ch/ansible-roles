---

dependencies:
  - role: build
    tags: [gpac, dependencies]

  - role: github
    tags: [gpac, dependencies]
    when: "'github' in gpac_repository_url"

  - role: local-source
    tags: [gpac, dependencies]
