---

dependencies:
  - role: github
    tags: [aeroo-docs, dependencies]
    when: "'github' in aeroo_docs_repository_url"

  - role: python
    tags: [aeroo-docs, dependencies]

  - role: libre-office
    tags: [aeroo-docs, dependencies]

  - role: supervisor
    tags: [aeroo-docs, dependencies]
