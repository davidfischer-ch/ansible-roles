---

dependencies:
  - role: requirements
    apt_requirements: '{{ php_packages }}'
    tags: [php, dependencies]
    ppa_requirements: '{{ (php_apt_repository|bool)|ternary([php_apt_repository], []) }}'
