---

dependencies:
  - role: requirements
    apt_requirements: '{{ php_packages }}'
    ppa_requirements: '{{ (php_apt_repository|length > 0)|ternary([php_apt_repository], []) }}'
    tags: [php, dependencies]
