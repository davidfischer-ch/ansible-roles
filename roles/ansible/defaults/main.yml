the_ansible_github_username: davidfischer-ch
the_ansible_modules: []
the_ansible_modules_packages:
  amazon: []
the_ansible_modules_python_packages:
  amazon:
    - awscli
    - boto
    - boto3
the_ansible_packages:
  - keychain
  - libffi-dev
  - libffi6
  - liblzma-dev
  - libssl-dev
  - openssl
  - sshpass
the_ansible_version: latest
the_ansible_from_source: no
