the_ansible_github_username: ansible
the_ansible_modules: []
the_ansible_modules_packages:
  amazon: []
the_ansible_modules_python_packages:
  amazon:
    - boto
    - boto3
the_ansible_packages:
  - keychain
  - libffi6
  - openssl
  - libffi-dev
  - libssl-dev
the_ansible_version: stable-2.1-fix
the_ansible_from_source: yes
