the_ansible_modules: []
the_ansible_packages:
  - keychain
  - libffi6
  - openssl
  - libffi-dev
  - libssl-dev
the_ansible_version: latest
the_ansible_modules_packages:
  amazon: []
the_ansible_modules_python_packages:
  amazon:
    - boto
