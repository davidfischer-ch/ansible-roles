---

# FIXME install package control, gitgutter ... configure ...

- name: Install programming packages
  package: >
    name={{ item }} state=present
  become: yes
  loop: '{{ programming_packages }}'
  tags: [development, packages]
