---

dependencies:
  - role: build
    tags: [development, dependencies]

  - role: ansible
    tags: [development, dependencies]

  - role: juju
    tags: [development, dependencies]

  - role: sublime-text
    tags: [development, dependencies]

  - role: vagrant
    vagrant_role_action: setup
    tags: [development, dependencies]
