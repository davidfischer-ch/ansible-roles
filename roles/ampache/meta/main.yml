---

dependencies:
  - role: composer
    tags: [ampache, dependencies]

  - role: github
    tags: [ampache, dependencies]

  - role: mysql
    tags: [ampache, dependencies]

  - role: openssl
    tags: [ampache, dependencies]
    when: ampache_ssl_enabled|bool

  - role: ufw
    ufw_rules: '{{ ampache_ufw_rules }}'
    tags: [ampache, dependencies]
    when: ampache_ufw_rules
