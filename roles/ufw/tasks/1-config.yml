---

- block:
    - name: Allow ssh
      ufw: >
        port={{ ssh_port }} proto=tcp rule=allow

    - name: Configure UFW
      ufw: >
        state=enabled logging={{ ufw_logging }} policy={{ ufw_policy }}
  notify: reload ufw
  become: yes
