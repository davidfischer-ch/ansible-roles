build_dynamic_defaults:
  centos:
    build_packages:
      # cleaned group Development Tools
      - autoconf
      - automake
      - binutils
      - bison
      - flex
      - gcc
      - gcc-c++
      - gettext
      - libtool
      - make
      - patch
      - pkgconfig
      # other packages
      - git
      - mercurial
      - subversion
  linux:
    build_packages:
      - build-essential
      - checkinstall
      - git
      - mercurial
      - pkg-config
      - subversion
  macosx:
    build_packages: []
